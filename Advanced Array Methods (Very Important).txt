📅 Day 9 – Advanced Array Methods (Very Important 🔥)

⏰ 2–3 Hours
🎯 Goal: Modern JavaScript style learn panna

🟢 1️⃣ forEach() – Loop Modern Way
let numbers = [10, 20, 30];

numbers.forEach(function(num) {
    console.log(num);
});

👉 Same as loop, but cleaner.

🟡 2️⃣ map() – Create New Array
let numbers = [1, 2, 3, 4];

let doubled = numbers.map(function(num) {
    return num * 2;
});

console.log(doubled);

👉 Output: [2, 4, 6, 8]

🟠 3️⃣ filter() – Filter Data
let numbers = [10, 15, 20, 25];

let even = numbers.filter(function(num) {
    return num % 2 === 0;
});

console.log(even);

👉 Output: [10, 20]

🔵 4️⃣ Arrow Function Version (Pro Style 😎)
let numbers = [1,2,3];

let result = numbers.map(num => num * 3);
console.log(result);

🟣 Difference Summary
Method	    Purpose
forEach	    Just loop
map	        Transform
filter	    Condition filter

💥 Practice Tasks (Compulsory)
1️⃣ Double all numbers using map
2️⃣ Filter odd numbers
3️⃣ Convert all names to uppercase
4️⃣ Find numbers greater than 50
5️⃣ Sum array using loop

🔥 Challenge

Given:

let students = [
  {name: "Vinesh", marks: 80},
  {name: "Arun", marks: 45},
  {name: "Kumar", marks: 90}
];

👉 Filter students with marks above 50
👉 Get only names using map

🎯 Today Target

✔ map & filter difference clear
✔ Arrow function practice
✔ Minimum 15 examples